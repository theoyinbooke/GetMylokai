# GetMylokai
This app uses Apple's FoundationModels framework for on-device AI inference. It requires iOS 26.0 or later. No account or sign-in is needed. All on-device features work without internet. Cloud AI features require the user to provide their own API keys in Settings.
